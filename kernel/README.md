# Kernel

The vanilla Linux source tree is downloaded into `build/src/linux-*`.

This directory is kept for project-level kernel notes. Kernel configuration
fragments live in `configs/kernel/` so the build remains reproducible and easy
to review.
